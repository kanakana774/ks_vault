全編通してsql_lesson_execution_planというDBを作成して環境を作成して。また、DDLも残しておいて。
全体通して、流れの見直し。表現の見直し。用語説明の見直し。シンプルさの見直し。（各項目をまずシンプルに説明し、詳細はすぐあとという流れ。）
- 17-1
	- hash indexは削除で、b-tree indexの説明だけに統一。
	- 各scan方式の説明を整理。特にbitmapIndex scan。
	- 借り物の図なので置き換え。（一番最後）
- 17-2
	- 構成の見直し。
	- index作成方法の見直し。
	- 実行計画の正確な読み方見直し。
	- ケーススタディの見直し。問題に集中できるようにできるだけ題材はシンプルにノイズを減らす。
	- indexが効かない落とし穴の例の見直し。（全項目残しておきたいわけではなく、有名で、よく引っかかるもの。説明はシンプルに。説明のマニアック度によっては削除。）
- 17-3
	- 実行計画の確認を一から作りたい。使用テーブルは相談。下記で全項目説明できれば研修で使用していてなじみがあるので、同じ構造を採用。データは新しく作成するが、内容は参考にしていい。研修生も実行できるようにDDLとかinsert文は別途残す。[[00_SQL研修/02_問題/08_結合から使用するテーブル]]
- 問題の改修[[00_SQL研修/03_解答/17_indexと実行計画]]
	- ハンズオン形式でいいが、ちゃんと教えたことが体感できる内容に。今はちょっと微妙。
- ~~[[15_トランザクションと排他制御]]の改修~~ → **対応済み**（2026-08-07）。`15_トランザクションと排他制御/` フォルダに [[15-1_導入_トランザクションとACID]] / [[15-2_実践_ロックとMVCC]] / [[15-3_補足_PLpgSQLの中のトランザクション制御]] として再構成。演習8問・解答・講師カンペも新設
- ~~[[12_PLpgSQL]]の改修~~ → **新版を並行作成**（2026-08-12）。**旧版（`PLpgSQL簡略版/` `PLpgSQL詳細版/`・`02_問題/12_PLpgSQL.md` `13_プロシージャ.md`・対応する解答）は一切変更せず温存**。直近の講義は旧版で回し、検証後に差し替える判断をする。
	- 講義版（4章＋応用1章に再編）: `12_PLpgSQL/PLpgSQL簡略版_改訂/` … [[12-1_PLpgSQLの基礎とDOブロック]] / [[12-2_変数とSQLの実行]] / [[12-3_制御構造]] / [[12-4_関数とプロシージャと例外処理]] / [[12-5_トリガーと動的SQL]] ＋ README（前提・章一覧・演習対応表）
	- 演習: [[00_SQL研修/02_問題/12_PLpgSQL_改訂]]（6問）/ [[00_SQL研修/02_問題/13_プロシージャ_改訂]]（5問）＋ 解答2本
	- 講師用詳解: `04_補足/講師用/講義の補足/12_PLpgSQL詳解/`（マニアック深掘り。配布物ではない）
	- 主な変更: ①**使用テーブルを 8章のECスキーマに統一**（旧版は独自の employees/departments で研修全体から浮いていた）②**先出し禁止**（説明前の構文を一切使わない順序に再設計）③全コード例を PostgreSQL 17 で実測し出力を掲載 ④**列名衝突（ambiguous column）・トリガー・動的SQLを追加**、JSONB・配列・VARIADIC・ASSERT 等の過剰トピックを講義から外して詳解版へ ⑤命名規約（`v_`/`p_`/`r_`）を講義・演習・解答で統一
	- 旧版で見つかった誤り（新版では再生産しない）: `ERRCODE='P0001'` を「独自コード」と説明（P0001 は既定値）／`%ROWTYPE` は「コンパイル時に列名チェック」（実際は初回実行時）／`RETURNING` で「更新前後の値を取得」（更新後のみ）／代入 `=` の「9.4以降」というバージョン条件（裏付けなし）／ログ`INSERT`と`RAISE;`再スローの両立（再スローでログも巻き戻る・実測）／`ELSIF` のみ有効という記述（`ELSEIF` も通る・実測）