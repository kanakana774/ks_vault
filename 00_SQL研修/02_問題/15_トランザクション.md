# 15章 演習：トランザクションと排他制御

問題1〜6は**手を動かして挙動を確かめる**ハンズオンです。問題7・8は**PL/pgSQL での実装**です。

---

## 準備

### 使用するテーブル

**8章以降と同じテーブルをそのまま使います。** 新しく作るものはありません。
まだ作っていない場合は [[08_結合から使用するテーブル|08_結合から使用するテーブル]] で作成してください。

### 窓を2つ開く

問題1・3・4・5・6は**クエリツールを2つ開いて**行います。片方を**窓A**、もう片方を**窓B**とします。

- **pgAdmin**：同じDBを右クリック →「クエリツール」を2回開く
- **psql**：ターミナルを2つ開いて、それぞれ接続する

> ⚠️ **同じタブで交互に打っても再現しません。** 接続が2本あることが前提です。

### リセットSQL

値が分からなくなったら、これを実行すれば初期状態に戻ります。

```sql
ROLLBACK;   -- 開きっぱなしのトランザクションがあれば閉じる

UPDATE products  SET price = 1200.00 WHERE product_id = 1;
UPDATE products  SET price =  800.00 WHERE product_id = 2;
UPDATE inventory SET stock_quantity = 50 WHERE product_id = 1 AND warehouse_id = 1;
DELETE FROM orders WHERE order_id = 999;
```

---

## 問題1：【2窓】行ロックと待機

書き込み同士がぶつかったとき何が起きるかを体験します。

**【手順】**

1. **窓A** で実行します。**`COMMIT` はまだ打ちません。**
   ```sql
   BEGIN;
   UPDATE products SET price = price + 100 WHERE product_id = 1;
   ```
2. **窓B** で実行します。
   ```sql
   UPDATE products SET price = price + 50 WHERE product_id = 1;
   ```
3. **窓Bが返ってこないこと**を確認します（実行ボタンが「実行中」のまま／プロンプトが戻らない）。
4. **窓A** で `COMMIT;` を実行します。
5. **窓Bが即座に完了すること**を確認します。
6. 価格を確認します。
   ```sql
   SELECT price FROM products WHERE product_id = 1;
   ```

**【問い】**

1. 最終的な価格はいくらになりましたか。
2. **なぜその値になったのか**を説明してください。特に、**窓Bは「1200」と「1300」のどちらを元に +50 したのか**、そう考える理由も書いてください。
3. 窓Bはなぜ待たされたのですか。**ロックの言葉を使って**説明してください。

#### 解答
```

```

---

## 問題2：【1窓】中断したトランザクションから復帰する

実務で必ず一度は踏むエラーと、その回避方法を体験します。

**【手順】**

1. 次を上から順に実行し、**出力を1行ずつ確認**してください。
   ```sql
   BEGIN;
   UPDATE products SET price = 1500 WHERE product_id = 1;
   SELECT 1/0;                            -- わざとエラーを起こす
   SELECT 'この行は実行されるか?';
   COMMIT;
   ```
2. 価格を確認します。
   ```sql
   SELECT price FROM products WHERE product_id = 1;
   ```

**【問い】**

1. 3行目のエラーの後、4行目の `SELECT` はどうなりましたか。**表示されたエラーメッセージをそのまま書いてください。**
2. `COMMIT;` と打ったのに、返ってきた文字列は何でしたか。
3. `UPDATE` の結果は残りましたか。
4. **【本題】** 上の一連の処理を、**「`UPDATE` は活かしたまま、`SELECT 1/0` の失敗だけをなかったことにして、続きを実行する」**形に書き換えてください。
   ヒント：[[15-1_導入_トランザクションとACID|15-1]] §4 で学んだコマンドを使います。

#### 解答
```sql

```

---

## 問題3：【2窓】読み取りは待たされない（MVCC）

**【手順】**

1. **窓A** で実行します。**`COMMIT` はまだ打ちません。**
   ```sql
   BEGIN;
   UPDATE products SET price = 120 WHERE product_id = 1;
   ```
2. **窓B** で実行します。
   ```sql
   SELECT price FROM products WHERE product_id = 1;
   ```
3. **窓A** で `COMMIT;` を実行します。
4. **窓B** でもう一度同じ `SELECT` を実行します。

**【問い】**

1. 手順2の `SELECT` は待たされましたか。返ってきた値はいくつでしたか。
2. 手順4ではいくつでしたか。
3. 問題1では窓Bが待たされたのに、ここでは待たされませんでした。**この違いはなぜ生まれるのですか。**
4. PostgreSQL は、まだ確定していない変更を持ちながら、他の人に古い値を見せています。**そんなことが可能なのはなぜですか。**（キーワード：MVCC）

#### 解答
```

```

---

## 問題4：【2窓】在庫が壊れる ── ロストアップデート

**この章で一番実務に直結する問題です。**

在庫50個の商品に、2人の顧客がほぼ同時に注文しました。**窓Aが5個、窓Bが3個**です。正しい結果は `50 - 5 - 3 = 42` のはずです。

アプリケーションは次の3ステップで在庫を引きます（よくある実装です）。

```
① 在庫を SELECT で読む
② アプリ側で「読んだ値 − 注文数」を計算する
③ 計算結果を UPDATE で書き戻す
```

**【手順】**

1. 在庫を初期化します。
   ```sql
   UPDATE inventory SET stock_quantity = 50 WHERE product_id = 1 AND warehouse_id = 1;
   ```
2. **窓A** で、①と②まで実行します。
   ```sql
   BEGIN;
   SELECT stock_quantity FROM inventory WHERE product_id=1 AND warehouse_id=1;
   -- 読めた値をメモする。アプリが「読めた値 - 5」を計算したことにする
   ```
3. **窓B** でも、①と②まで実行します。
   ```sql
   BEGIN;
   SELECT stock_quantity FROM inventory WHERE product_id=1 AND warehouse_id=1;
   -- 読めた値をメモする。アプリが「読めた値 - 3」を計算したことにする
   ```
4. **窓A** で③を実行して確定します。**計算結果を絶対値で書き込みます。**
   ```sql
   UPDATE inventory SET stock_quantity = 45 WHERE product_id=1 AND warehouse_id=1;
   COMMIT;
   ```
5. **窓B** でも③を実行して確定します。
   ```sql
   UPDATE inventory SET stock_quantity = 47 WHERE product_id=1 AND warehouse_id=1;
   COMMIT;
   ```
6. 結果を確認します。
   ```sql
   SELECT stock_quantity FROM inventory WHERE product_id=1 AND warehouse_id=1;
   ```

**【問い】**

1. 最終的な在庫はいくつになりましたか。**42 になりましたか。**
2. どちらの注文が消えましたか。**なぜ消えたのか**を、手順2・3で両方が読んだ値に着目して説明してください。
3. **【本題】** 手順2〜5を**修正して、正しく 42 になるようにしてください。** 追加するのは1箇所だけです。
4. 修正版では、**窓Bはどの手順で待たされるようになりましたか。** 修正前（問題1）と比べて、待つ位置がどう変わったか説明してください。
5. そもそも `UPDATE inventory SET stock_quantity = stock_quantity - 5 ...` と1文で書けば、この問題は起きません。**では、なぜ実務で3ステップの実装が必要になるのですか。**（ヒント：在庫が足りなかったら注文を断りたい）

#### 解答
```sql

```

---

## 問題5：【2窓】デッドロックを起こして、直す

**【手順（起こす）】**

1. **窓A** で実行します。
   ```sql
   BEGIN;
   UPDATE products SET price = price + 1 WHERE product_id = 1;
   ```
2. **窓B** で実行します。
   ```sql
   BEGIN;
   UPDATE products SET price = price + 1 WHERE product_id = 2;
   ```
3. **窓A** で実行します（**返ってこなくなります**）。
   ```sql
   UPDATE products SET price = price + 1 WHERE product_id = 2;
   ```
4. **窓B** で実行します。
   ```sql
   UPDATE products SET price = price + 1 WHERE product_id = 1;
   ```
5. **約1秒待ちます。**

**【問い】**

1. 手順5の後、何が起きましたか。**エラーメッセージをそのまま書いてください。** また、**どちらの窓が犠牲になりましたか。**
2. なぜこの状態になったのかを、「窓Aは○○を待っている／窓Bは××を待っている」の形で説明してください。
3. PostgreSQL はこの状態を検知するまで**約1秒待ちました。** すぐに検知しないのはなぜだと思いますか。次のSQLの結果も踏まえて答えてください。
   ```sql
   SHOW deadlock_timeout;
   ```
4. **【本題】** 上の手順を**デッドロックが起きないように書き換えてください。** SQLの中身は変えず、**順番だけ**を変えます。
5. 4の考え方を、開発チームのルールとして一文で表現してください。

#### 解答
```

```

---

## 問題6：【2窓】固まったときに犯人を探す

問題1の状態（窓Aが `COMMIT` せず、窓Bが待たされている）を**もう一度作ってください。**
その状態のまま、**3つ目の窓**（または待っていない側の窓）から調査します。

**【手順】**

1. 待たされている接続と、待たせている接続を探します。
   ```sql
   SELECT pid,
          pg_blocking_pids(pid) AS 待たせている相手,
          wait_event_type       AS 待ちの種類,
          left(query, 50)       AS 実行中のSQL
   FROM pg_stat_activity
   WHERE cardinality(pg_blocking_pids(pid)) > 0;
   ```
2. 犯人のPIDが分かったら、その接続が何をしているか調べます。
   ```sql
   SELECT pid, state, xact_start, now() - xact_start AS 経過時間, left(query, 80)
   FROM pg_stat_activity WHERE pid = <調べたPID>;
   ```

**【問い】**

1. 「待たせている相手」に表示されたPIDは、どちらの窓のものでしたか。各窓で `SELECT pg_backend_pid();` を実行して確認してください。
2. 手順2で犯人の `state` は何と表示されましたか。**その状態が意味することを説明してください。**
3. この犯人を止める手段が2つあります。それぞれ何をするコマンドで、どう違いますか。
   ```sql
   SELECT pg_cancel_backend(<PID>);
   SELECT pg_terminate_backend(<PID>);
   ```
4. 本番環境でこれらを実行する前に、**確認すべきこと**を挙げてください。

#### 解答
```

```

---

> [!CAUTION]
> **ここから先は難しめです。** 一旦模範解答を見て、それを解読したり写経したりするのもアリです。
> 難しそうと感じたら相談してください。

---

## 問題7：在庫引き当てを伴う注文処理

問題4で手作業でやったことを、**プロシージャとして実装**します。

顧客から注文を受けた際、在庫をチェックして注文を確定させるプロシージャ `proc_create_order` を作成してください。

**【仕様】**

1. **引数**: `p_customer_id`（顧客ID）, `p_product_id`（商品ID）, `p_warehouse_id`（倉庫ID）, `p_quantity`（注文数）
2. **排他制御**: 在庫確認から更新までの間に他のセッションが同じ在庫を書き換えないよう、`INVENTORY` の該当行に**行ロックを取得**すること（**問題4の答えがそのまま使えます**）
3. **在庫チェック**:
   - 指定された倉庫に十分な在庫があるか確認する
   - 在庫が不足している場合は `RAISE EXCEPTION` で処理を中断する
     ※難しければ `RAISE NOTICE` でログを出すだけでも可
4. **注文処理**:
   - `ORDERS` に新規レコードを挿入（`STATUS` は `'Pending'` 固定）
   - `ORDERITEMS` に明細レコードを挿入
   - `INVENTORY` の `STOCK_QUANTITY` を注文数ぶん減算
5. **一貫性**: いずれかでエラーが起きたら全ての変更が無効になること

> 💡 **`EXCEPTION` ハンドラの中に `ROLLBACK` を書かないでください。** 理由は [[15-3_補足_PLpgSQLの中のトランザクション制御|15-3]] §3 にあります。**書かなくても巻き戻ります。**

#### 解答
```sql

```

#### テスト実行例

**正常系：**
```sql
CALL proc_create_order(1, 1, 1, 5);
-- ORDERS, ORDERITEMS が増え、INVENTORY の在庫が 50→45 に減っていることを確認
```

**異常系（在庫不足）：**
```sql
CALL proc_create_order(1, 9, 1, 10);
-- 商品ID:9 (Luxury Sofa) は在庫2しかないため、エラーになりテーブルは更新されない
```

**【発展】** 窓を2つ開いて、**同じ商品に対して同時に `CALL`** してみてください。片方が待たされることを確認しましょう。

---

## 問題8：未発送注文の一括発送バッチ（分割コミット）

`STATUS = 'Processing'` の注文を「出荷済み（Shipped）」に更新するバッチ `proc_batch_shipping` を作成してください。

**【仕様】**

1. **対象抽出**: `ORDERS` から `STATUS = 'Processing'` の注文をすべて抽出する
2. **ループ処理**: 1件ずつループで処理する
3. **更新内容**:
   - `SHIPMENTS` に新規レコードを作成（`Status`: `'In Transit'`, `Shipped_Date`: 当日）
   - `ORDERS` の `STATUS` を `'Shipped'` に更新
4. **分割コミット**: **1件処理が終わるたびに `COMMIT`** すること
5. **擬似エラー**: 学習のため、`order_id = 13` のときだけ意図的にエラーを起こし、**「エラーが起きても、それより前にコミットされたデータは消えない」**ことを確認する

> 💡 **`COMMIT` を書く位置に注意してください。** `EXCEPTION` 句を持つブロックの**本体**に書くと `cannot commit while a subtransaction is active` になります。→ [[15-3_補足_PLpgSQLの中のトランザクション制御|15-3]] §5 のテンプレート

#### 解答
```sql

```

#### 体験の手順

##### 1. 事前データの確認
`STATUS = 'Processing'` の注文は ID 7 と ID 13 です。
```sql
SELECT order_id, status FROM orders WHERE status = 'Processing' ORDER BY order_id;
```

##### 2. プロシージャの実行
```sql
CALL proc_batch_shipping();
```

**出力ログ（イメージ）：**
> NOTICE: 注文ID: 7 の発送処理に成功しました。
> WARNING: 注文ID: 13 でエラーが発生しました。この件をスキップします。エラー: ID:13で予期せぬエラーが発生しました！

##### 3. 結果の確認

```sql
SELECT order_id, status FROM orders WHERE order_id IN (7, 13) ORDER BY order_id;
```

| order_id | status | 備考 |
| :--- | :--- | :--- |
| 7 | **Shipped** | 成功。`COMMIT` されたので変更が残っている |
| 13 | **Processing** | 失敗。この件だけ元に戻っている |

**【問い】**

1. `BEGIN; CALL proc_batch_shipping(); COMMIT;` のように、**外側をトランザクションで囲んで**実行するとどうなりますか。実際に試して、エラーメッセージを書いてください。
2. なぜそうなるのですか。→ [[15-3_補足_PLpgSQLの中のトランザクション制御|15-3]] §2-1
3. 100万件を1つの `BEGIN ... COMMIT` で処理した場合の問題を、**2つ**挙げてください。（ヒント：ひとつは [[15-2_実践_ロックとMVCC|15-2]] §1-1 で体験したことです）
