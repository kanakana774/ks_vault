# 段③ 決定 — やること

> **この段の目的と手順を持つ。** 判断の経緯は `07_実施計画.md` §4 が正本。

---

## 目的

> ### 決めることと、聞くことを、着手前に仕分けている。

**到達した状態**: 何を作るか・どうなれば完了かを、**AIに触る前に**確定させている。

### 段②との違い

| 段 | すること |
|---|---|
| ② 把握 | **気づく**（何が決まっていないかを分かる） |
| **③ 決定** | **決める**（決めたものを渡し、返ってきた計画と条件を判定する） |

**段②の提出物（Q&A票）が、そのままこの段の入力になる。**

### この段の2つの作業は性質が違う

| 作業 | 性質 |
|---|---|
| 9 決定事項を書き戻す | **渡すもの** |
| 10 計画と受入条件をレビューする | **返ってきたものを判定する**（＝段③の中の小さな段⑤） |

**受入条件は白紙から書かせない。** 実務ではAIに出させて人がレビューする。**人が足すのは、AIが出せないもの（不変条件・非機能）だけ。**

---

## 作業

| 作業 | やること | 目安 | この作業の目的 |
|---|---|---|---|
| **9** | **書き戻す** — Q&A票で②に振った項目を `決定事項.md` に書き、地雷を `CLAUDE.md` に書く | 15分 | AIが同じ空白をまた勝手に埋めるのを止める |
| **10** | **計画と受入条件を出させて、レビューする** — 6つの観点で判定し、**目視でしか判定できないものを自分で足す** | 30分 | **止められるのは着手前だけ** |

**提出物**: `決定事項.md` ／ 承認した計画 ／ **自分が足した受入条件**

---

## 作業9の行き先（3つに振り分ける）

| 段②の判定 | 行き先 | 理由 |
|---|---|---|
| **② 自分で決めた** | **`決定事項.md`**（新規・研修生が書く） | 仕様の決定なので、規約とは別の場所に置く |
| **③ 聞く** | **Q&A票に残す。AIに渡さない** | 渡した時点で独断になる |
| **やってはいけないこと** | **`CLAUDE.md`** | これは規約なので指示ファイルが正しい |

**`CLAUDE.md` に書くのは1行だけ**: 「実装の前に `決定事項.md` を必ず読むこと」

### なぜ決定を別ファイルにするのか（実務との対応）

| 実務の呼び名 | 中身 |
|---|---|
| **ADR**（Architecture Decision Record）／`docs/adr/` | 決定と理由を残すファイル |
| 指示ファイル（`CLAUDE.md` / `AGENTS.md`）の作法 | **短く保ち、詳細は別ファイルへのリンクにする**（長いと守られなくなる） |

**「指示ファイルを短く保って参照を張る」ことは実務の定石。** ただし**置き場は現場ごとに違う**（チケットのコメント・設計書の追記・ADR）。研修生に伝えるのは2点だけ。

> **① 規約と決定を分ける** ／ **② AIに読ませる経路を用意する**

**設計書に直接書かない理由**（研修都合ではなく実務的な理由）:
**上流が決めたことと、自分が決めたことが混ざる。** 決定ログなら「**これは自分が決めた**」という出所が残り、段⑥のPRで説明する材料になる。独断かどうかの検証もできる。

---

## 作業10のレビュー観点（5つ ＋ 1つ）

```
□ 層の順序（Entity→Mapper→Service→Controller）になっているか
□ 触るファイル一覧は妥当か。頼んでいないものが入っていないか
□ 設計書に無い判断が混ざっていないか
□ 確認方法（どう動かして確かめるか）が書かれているか
□ 後戻りできる単位に割れているか
□ 目視でしか判定できないものが抜けていないか  ← これが自分で足す分
```

**AIが出せるもの／人が足すものの線**

| | 誰が出すか |
|---|---|
| 正常系・境界値・異常系 | **AIが出す**（仕様から機械的に導出できる） |
| **不変条件**（頼んでいないテーブル・ファイルを変えない） | **人が足す** |
| **非機能**（クエリの本数・外に出してはいけない情報） | **人が足す**（**2か月目のレビュー観点がそのまま使える**） |

**観点を渡さないと「はい」を押すだけになる。** **承認するまで着手させない。**

---

## 講師がやること

| 作業 | 講師 |
|---|---|
| 9 | `決定事項.md` の雛形と `CLAUDE.md` の記入欄を配る。**中身は添削しない** |
| 10 | **観点6つを配る。** そのうえで「なぜその線を引いたのか」を聞く（＝議論。これが実質のフィードバック）。**⑥が空のまま通っていないかを見る** |
